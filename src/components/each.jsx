

export default function WayToEach({ title, description }) {
  return(
<ul>
        <li>
          <p>
            <strong>{title}</strong> {description}
            
          </p>
        </li>
      </ul>
  )
}